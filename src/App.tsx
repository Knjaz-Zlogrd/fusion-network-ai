import * as React from "react"
import { Flex, Text } from "@chakra-ui/react"
import { Route, Routes } from 'react-router-dom';

export const App = () => (
  <Flex>
    <Text>React Aplikacija</Text>
  </Flex>
)
